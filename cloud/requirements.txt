# swafra cloud edge (in addition to engine/requirements.txt)
# mcp 2.x currently drops mcp.server.fastmcp — stay on 1.x SDK.
mcp>=1.25.0,<2
PyJWT[crypto]>=2.10.0
pydantic-settings>=2.0.0
psycopg[binary,pool]>=3.2
httpx>=0.27.0
starlette>=0.40.0
