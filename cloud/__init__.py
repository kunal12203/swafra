"""swafra cloud edge — remote Streamable HTTP MCP with verified multi-tenancy.

Additive package: nothing here is imported by the local stdio path
(`swafra serve`), so local users are unaffected. See docs/adr/0001-0004.
"""
